# fastlane Appfile — shared app identifiers.
# Secrets come from fastlane/.env (gitignored); see fastlane/.env.example.

app_identifier("app.tasfer") # iOS bundle id

for_platform :android do
  package_name("app.tasfer")
  json_key_file(ENV["PLAY_JSON_KEY_PATH"]) if ENV["PLAY_JSON_KEY_PATH"]
end
